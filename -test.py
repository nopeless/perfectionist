import time









start = time.time()

a=0
for i in range(10):
	# pass
	sorted(list(range(999999,-1,-1)))

print(-start+(start:=time.time()))

for i in range(10):
	sorted(list(range(1000000)))

print(-start+(start:=time.time()))
print("-"*100)
